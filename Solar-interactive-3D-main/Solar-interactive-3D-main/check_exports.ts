import * as Hands from '@mediapipe/hands';
import * as Camera from '@mediapipe/camera_utils';
console.log('Hands default keys:', Object.keys((Hands as any).default));
console.log('Camera default keys:', Object.keys((Camera as any).default));
